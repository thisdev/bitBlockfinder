#pragma once

/* Kleiner HTTP-Dienst, der Bildschirmaufnahmen und den Seitenwechsel
 * anbietet -- damit Abbildungen fuer die Dokumentation nicht abfotografiert
 * werden muessen. Standardmaessig abgeschaltet. */
void shot_start(void);
